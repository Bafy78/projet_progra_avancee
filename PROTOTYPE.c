float Eval(Arbre *A,x){
	switch(A.jeton.lexem){
	case REEL :case VAR :{
		return(a.jeton.valeur);
		break
	}
	case oper; {
		switch(A.jeton.valeur.oper)
		}
		case PLUS:{
			return(Eval(A->lg)+Eval(A->ld))
		}	
		case MOINS:{
			
	case Function{
		switch(A.jeton.valeur.)
			}
		case SIN;{
			return (sin (Eval(A->lg);
			break;
			
			
			
}
